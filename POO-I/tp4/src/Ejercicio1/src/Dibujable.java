/**
 * Interfaz para objetos que pueden ser dibujados.
 * Método dibujar() imprime un mensaje indicando que la figura se está dibujando.
 */
public interface Dibujable {
    /**
     * Indica que la figura se está dibujando (impresión en consola).
     */
    void dibujar();
}
